let a = 10;
let b = 15;

function test(x, y) {
    if (x >= y) {
        console.log(`${x} is larger or equal than ${y}`);
    } else {
        console.log(`${y} is larger than ${x}`);
    }
}

test(a , b)

// switch (key) {
//     case value:
        
//         break;

//     default:
//         break;
// }
