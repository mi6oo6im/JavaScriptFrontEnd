function solve(num, pow) {
    let toPoewer = (x, y) => Math.pow(x, y);
    console.log(toPoewer(num, pow));
}

solve (2,8);
console.log('----------------');
solve (3,4);
console.log('----------------');